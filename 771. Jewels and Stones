class Solution {
public:
    int numJewelsInStones(string J, string S) {
        int jewelsCount = 0;
        
        for(auto c : S)
        {
            if (J.find(c) != string::npos)
                jewelsCount++;
        }
        
        return jewelsCount;
    }
};
